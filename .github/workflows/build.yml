name: Build App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pillow reportlab openpyxl pyinstaller

      - name: Convert PNG to ICO
        run: |
          python -c "
          from PIL import Image
          img = Image.open('assets/logo/App_Logo.png').convert('RGBA')
          img.save('app_icon.ico', format='ICO', sizes=[(16,16),(32,32),(48,48),(64,64),(128,128),(256,256)])
          "

      - name: Build EXE
        run: |
          pyinstaller --noconfirm --onefile --windowed `
            --name "DesktopQC" `
            --icon "app_icon.ico" `
            --add-data "assets;assets" `
            --add-data "screens;screens" `
            --add-data "reports;reports" `
            --hidden-import "PIL._tkinter_finder" `
            main.py

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v5
        with:
          name: DesktopQC-windows
          path: dist/DesktopQC.exe

  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pillow reportlab openpyxl pyinstaller

      - name: Build macOS App
        run: |
          pyinstaller --noconfirm --onedir --windowed \
            --name "DesktopQC" \
            --icon "assets/logo/AppIcon.icns" \
            --add-data "assets:assets" \
            --add-data "screens:screens" \
            --add-data "reports:reports" \
            --hidden-import "PIL._tkinter_finder" \
            main.py

      - name: Zip .app bundle
        run: |
          cd dist
          zip -r DesktopQC-macos.zip DesktopQC.app

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v5
        with:
          name: DesktopQC-macos
          path: dist/DesktopQC-macos.zip
